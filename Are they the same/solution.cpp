/* this solution will fetch you a score of 89.53 */
/* you have minimise the code length */
/* if anybody has a better solution, you can show us */

#include<bits/stdc++.h>
using namespace std;main(){string s,S;cin>>s>>S;sort(s.begin(),s.end());sort(S.begin(),S.end());if(s==S)cout<<"YES";else cout<<"NO";}
